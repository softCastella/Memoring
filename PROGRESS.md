# Memoring 진행 기록

## 현재 상태
1차 앱(오늘/기록/꾸미기/페이지/로컬 저장)과 운영자용 **로컬 배경 관리 웹**, 앱 안 **배경 스토어 UI**까지 있다.
기본 테마는 사진 없는 로즈(웜 아이보리, 브라운 글자, 더스티 로즈)다.
**막힌 것:** 관리자(`localhost:8081`)에서 공개한 팩이 앱(`localhost:8084`) 스토어에 안 들어온다. 2026-09-18 아침에 이어서 본다.

## 구현된 것
- Flutter Android 우선 앱. 앱 이름은 `lib/config/app_config.dart`의 `AppConfig.appName`
- 오늘: 주간 날짜 줄, 분류 탭, 할 일·매일 반복이 한 목록, 주제 페이지, 앞머리 드래그 정렬, 제목만 취소선
- `+ 페이지` / `+ 항목` 타원 뱃지는 페이지 줄에 같이 둔다. 위쪽 단독 `+`는 빼 두었다
- 기록: 지침·차분·평온·설렘·기쁨, 한 줄 메모, 저장, 지난 기록
- 꾸미기: 로즈/세이지/오트/나이트, 개인 배경, 미리보기 후 적용, 기본 복원
- 설정: 꾸미기, 배경 스토어, 내 배경, 기본 디자인 복원
- 본문 폰트: 고운바탕 (SIL OFL, `fonts/`)
- 로컬 JSON 저장. 첫 실행 데이터는 비어 있음. 미리보기 `--dart-define=USE_SAMPLE_DATA=true`
- 관리자 웹 `lib/admin_main.dart` 포트 8081: 배경팩 생성/수정, 이미지, 무료·유료(스토어 상품 ID), 비공개/공개
- 앱 배경 스토어 UI: 공개 팩, 카테고리, 무료만 적용. 유료는 서버 검증 전 잠금
- Android 홈 위젯 소스(소형 진행률, 중형 목록). 실기기 확인은 못 함
- 웹 미리보기 포트는 8084로 유지. 바깥 다크 프레임이 로즈 화면을 덮지 않게 수정

## 실행
앱:

```bash
flutter run -d web-server --web-hostname localhost --web-port=8084 --dart-define=USE_SAMPLE_DATA=true
```

관리자 배경 관리:

```bash
flutter run -d web-server --web-hostname localhost --web-port=8081 -t lib/admin_main.dart
```

## 아직 안 한 것
- 관리자 공개 팩 → 앱 배경 스토어 반영 (아침 우선)
- 실제 기기 Android 홈 화면 위젯 확인
- 외부 서버, 공개 배포된 관리자 웹, 운영 인증
- 결제 / 구매 복원 / 구독
- 사용자 개인 할 일·감정 조회 (의도적으로 없음)

## 다음 단계에 필요한 외부 설정
- Android cmdline-tools / 에뮬레이터 또는 실기기
- 홈 위젯: 실기기에서 홈 화면 추가 확인
- 배경 스토어: 호스팅, 구매 검증 서버, Play 상품 ID
- 관리자 웹 운영 배포 시 실제 인증과 이미지 스토리지

## 검증
`flutter test`로 단위·위젯 테스트를 확인한다. 관리자→스토어 브라우저 연동과 갤러리·실기기 키보드는 이 환경에서 끝나지 않았다.
